name: syeh-brave-cadmium-manatee
runtime: yaml
description: A minimal TypeScript Pulumi program

resources:
  randomInt:
    type: random:RandomInteger
    properties:
      min: 1
      max: 100

outputs:
  result: ${randomInt.result}
